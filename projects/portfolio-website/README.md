# Portfolio Website Starter

A static portfolio site built with HTML, CSS, and a small JavaScript project
list.

## Run

Open `index.html` directly in a browser, or serve the folder:

```bash
python3 -m http.server 8000
```
