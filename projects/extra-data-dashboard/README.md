# Data Dashboard Starter

A static dashboard that loads local JSON and renders summary metrics.

## Run

Because this project uses `fetch`, serve the folder instead of opening the file
directly:

```bash
python -m http.server 8000
```

Then visit `http://127.0.0.1:8000`.
