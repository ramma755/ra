# Canvas Game Starter

A small browser game built with the HTML canvas API.

## Run

Open `index.html` directly in a browser, or serve the folder:

```bash
python3 -m http.server 8000
```

## Controls

- Left/right arrows or A/D to move
- Space to restart after a collision
