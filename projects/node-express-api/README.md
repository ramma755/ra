# Node/Express API Starter

A small Express JSON API with health, list, and create endpoints.

## Run

```bash
npm install
npm run dev
```

## Endpoints

- `GET /health`
- `GET /api/projects`
- `POST /api/projects`

Example POST body:

```json
{
  "id": "new-project",
  "name": "New Project"
}
```
