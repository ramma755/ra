# Python CLI Tool Starter

An installable Python command-line tool with two commands.

## Install locally

```bash
python3 -m venv .venv
. .venv/bin/activate
python3 -m pip install -e .
```

## Use

```bash
project-tool stats "hello from the CLI"
project-tool slug "My New Project"
```
